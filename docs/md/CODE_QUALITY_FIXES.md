# Исправление проблем качества кода

## Найденные проблемы

### 1. ❌ Закомментированный код вместо исправления

**Файлы:**
- `backend/app/schemas/filament.py` - строки 122-128
- `backend/app/schemas/preset.py` - строки 115-121

**Проблема:**
```python
# FilamentWithBrand - временно закомментирован из-за проблем с forward references в OpenAPI
# TODO: Восстановить после исправления проблемы
# class FilamentWithBrand(FilamentResponse):
#     """Schema for Filament with Brand info."""
#     brand: "BrandResponse"
#     model_config = ConfigDict(from_attributes=True)
```

**Решение:**
- Либо исправить forward references (использовать `from __future__ import annotations` или `TYPE_CHECKING`)
- Либо удалить код, если он не используется

**Проверка использования:**
- Нужно проверить, используются ли `FilamentWithBrand` и `PresetWithFilament` где-то в коде

---

### 2. ❌ "Временно" решение без исправления

**Файл:** `backend/app/api/v1/endpoints/auth.py` - строки 908-923

**Проблема:**
```python
# TODO: Реализовать подтверждение через код
# Вместо немедленного обновления email, сохранить новый email в pending_email
# и отправить код подтверждения на новый email
# Email обновляется только после подтверждения кода

# Временно: обновляем email и сбрасываем верификацию (требуется повторная верификация)
current_user.email = data.new_email
current_user.email_verified = False
```

**Решение:**
- Добавить поле `pending_email` в модель User
- Сохранять новый email в `pending_email`
- Отправлять код подтверждения
- Обновлять email только после подтверждения кода

---

### 3. ❌ TODO комментарии как оправдание

**Файлы с TODO:**
- `backend/app/api/v1/endpoints/auth.py`:
  - Строка 147: Отправка email с токеном верификации
  - Строка 726: Отправка email с токеном восстановления
  - Строка 908: Реализация подтверждения через код
  - Строка 922: Отправка email с кодом подтверждения

**Решение:**
- Либо реализовать функционал (отправка email)
- Либо удалить TODO, если это не критично для MVP
- Либо создать задачу в roadmap

---

## План исправлений

### Приоритет 1 (критично):
1. ✅ Удалить закомментированный код `FilamentWithBrand` и `PresetWithFilament`, если не используется
2. ✅ Исправить "временно" решение с обновлением email

### Приоритет 2 (важно):
3. ⏳ Проверить все TODO и либо реализовать, либо удалить

---

## Реализация

См. изменения в коммитах.

